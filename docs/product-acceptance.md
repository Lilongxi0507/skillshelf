# 本机共享版验收矩阵

本文件记录本次产品改造的证据，不继承历史版本的 CI 通过状态。生成候选包不等于公开发布。

## 验收范围

| 场景 | 必须观察到的结果 | 本次状态 |
| --- | --- | --- |
| 分类与搜索 | 十二类用途；成员命中归属父包；中文/英文用途检索；全栈结果确实符合用途 | Linux 隔离测试通过；PTY 已验证子技能搜索命中父包 |
| 完整安装 | Taste 全部 13 成员和资源；失败不提交半套；安装一包仅下载一次 | Linux 隔离测试通过 |
| 版本生命周期 | 更新、固定、回滚与移除以整包为单位；成员入口随版本一致切换 | Linux 隔离测试通过 |
| 同机共享 | 两个目录入口解析至同一存储成员；第二目标接入不重复获取包 | Linux 隔离测试通过 |
| 接入状态 | 注册、配置、原生读取验证和待重载各自有证据；重复接入不增实例 | Linux 隔离测试通过；OpenCode 正文读取通过，Hermes 仅发现，其他 Agent 待实测 |
| 私有技能去重 | 完整树相同才可替换；确认后备份；异内容、外部归属、并发修改保留 | Linux 隔离测试通过 |
| 创作与导入 | 单技能和多成员包可校验入库；跨成员引用、来源和许可保留 | Linux 隔离测试通过 |
| 共同发现 | 本地新包出现在索引及已接入 Agent 范围，按任务读取对应成员 | 本地索引和 Linux 测试通过；第二 Agent 原生发现待实测 |
| 任务组合 | 引用保护、冲突检测、成员偏好；多个组合共用安装内容 | Linux 隔离测试通过 |
| 项目锁 | 全局更新不改变冻结项目；离线恢复精确版本；旧锁显式迁移 | Linux 隔离测试通过 |
| MCP 共用 | 两个真实协议会话使用同一运行安装；初始化和工具调用可观察 | 中央定义、诊断和预览已通过；真实 MCP 会话待实测 |
| 原生配置 | 写前差异和备份；保留无关配置；变更过的受管项卸载时保留 | 安全预览与夹具通过；原生配置适配器待平台实测 |
| 目录迁移 | 中文/空格路径；内容校验；受管入口更新；中断恢复 | Linux 流程通过；跨平台和中断矩阵待补 |
| 完整卸载 | 仅清理归属明确的项目；选定导出保留；剩余项和 npm 步骤准确报告 | Linux 预览与受管投影清理通过；跨平台卸载待实测 |
| 实际 npm 包 | 真实 tarball 安装、真实命令入口完成生命周期；清单与包字节相符 | CLI tarball 审计和安装 smoke `1/1` 通过；尚未发布 npm |
| 终端交互 | 40/80/120 列、无颜色、取消、返回、多选与 JSON 自动化 | Linux PTY 4 组通过 |

## 平台证据

| 平台 | Node 22 | Node 24 | 说明 |
| --- | --- | --- | --- |
| Linux | 未执行 | `160 passed, 0 failed, 2 skipped`；PTY 40/80/120 列通过 | Node 24 Docker 隔离；跳过 Windows ACL 与未注入 tarball 的安装测试 |
| Windows | 未执行 | 未执行 | 路径/ACL 模拟夹具不能代替原生系统验收 |
| macOS | 未执行 | 未执行 | POSIX 夹具不能代替原生系统验收 |

真实 CLI tarball 已在独立安装 smoke 中验证；Windows ACL 仍需 Windows 原生环境。

## Agent 原生证据

| Agent | 目录与配置适配测试 | Agent 实际加载测试 |
| --- | --- | --- |
| Codex | 适配器夹具通过 | 原生加载待执行 |
| Claude Code | 适配器夹具通过 | 原生加载待执行 |
| OpenCode | 适配器与原生目录读取通过 | 正文挑战通过 |
| DSH | 适配器夹具通过 | 本机不可用 |
| Cursor | 适配器夹具通过 | 本机不可用 |
| Hermes | 适配器与原生目录发现通过 | 正文挑战待执行 |
| 通用 CLI/MCP | 待验证 | 待验证协议调用 |

实际加载验收需要在对应原生程序中读取指定成员并返回校验挑战，记录程序版本、平台、时间、入口和摘要。目录存在、软链接创建或模型自述都不能替代该记录。

## 发布状态

本次仅授权本地开发、测试与候选包交付。尚未获准 npm 发布；不执行 npm publish、远端推送或生产部署。
